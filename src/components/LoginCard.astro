---

---

<>
    <div class="login-card">
        <div class="image-container">
            <img src="/cat.webp" alt="Profile Image" class="profile-image" />
        </div>
        <h2 class="text">Welcome to Smilbot</h2>
        <!-- TODO: Añadir un elemento 'a' con href al endpoint /auth/discord del back  -->
        <button class="login-button">Login with Discord</button>

        <!-- Background noise -->
        <div style="height: 0; width: 0; position: absolute; visibility: hidden;">
            <svg viewBox="0 0 200 200" xmlns='http://www.w3.org/2000/svg'>
                <filter id='noiseFilter'>
                    <feTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch' />
                </filter>
                <rect width='100%' height='100%' filter='url(#noiseFilter)' />
            </svg>
        </div>
    </div>
</>

<style>
    .login-card {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 60vw;
        height: 100vh;
        min-width: 350px;
        max-width: 500px;
        box-shadow: 0 10px 50px rgba(0, 0, 0, 1), inset 0 0 5px rgba(0, 0, 0, 0.6);
        align-self: center;
        justify-self: center;
        background-image: url('/public/noisy-texture-.png'), linear-gradient(0deg, #08090f, #171e27, #0f1720);;
        background-repeat: repeat;
        position: relative;
        overflow: hidden;
    }

    .image-container {
        display: flex;
        justify-content: center;
        align-items: center;
        margin-top: 20px;
        position: relative;
        width: 90%;
    }


    .image-container::before{
        content: '';
        position: absolute;
        top: 5.5%;
        width: 86%;
        height: 95.5%;
        background-color: transparent;
        box-shadow: rgba(0, 0, 0, 0.38) 1px 1px 3px 3px, inset rgba(255, 255, 255, 0.09) 0 0 2px 2px;
        border-radius: 50%;
        z-index: 0;
    }


    .profile-image {
        width: 85%;
        height: auto;
        position: relative;
        z-index: 1;
    }


    .text {
        margin-top: 20px;
        font-size: 18px;
        font-family: sans-serif;
        font-weight: bold;
        color: #fff;
    }

    .login-button {
        margin-top: 20px;
        padding: 15px 20px;
        border-radius: 25px;
        background-color: #df7030;
        color: #fff;
        font-size: 16px;
        font-weight: bold;
        font-family: sans-serif;
        cursor: pointer;
        border: 1px solid #7a6b5e;
    }
</style>